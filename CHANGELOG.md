# Changelog

本工程版本遵循语义化版本（MAJOR.MINOR.PATCH），发布节奏见 README「发布」。

## [0.1.0] - 生成日

- 由 agent-toolsmith `android-kotlin` 模板生成
- 自带：Compose UI 骨架、Retrofit 纵切（五条 CRUD 路径测试）、
  build-logic 约定插件、CI（lint → test → build）、spotless(ktlint)
